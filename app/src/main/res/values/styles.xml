<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AtyMainBottomTabText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">53dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_weight">1.0</item>
        <item name="android:textColor">@color/normalText</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="ObjViewer_Menu_Image">
        <item name="android:layout_width">@dimen/objviewer_image_wh</item>
        <item name="android:layout_height">@dimen/objviewer_image_wh</item>
        <item name="android:padding">@dimen/objviewer_image_padding</item>
        <item name="android:scaleType">centerCrop</item>
    </style>
</resources>
